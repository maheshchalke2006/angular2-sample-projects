import { SummarydirectiveDirective } from './summarydirective.directive';

describe('SummarydirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SummarydirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
