import { SimplepipePipe } from './simplepipe.pipe';

describe('SimplepipePipe', () => {
  it('create an instance', () => {
    const pipe = new SimplepipePipe();
    expect(pipe).toBeTruthy();
  });
});
